@using Helpers;
@using Common.Utilities;
@using ViewModels;
@model NewsViewModel;

@{
    Layout = "~/Views/Shared/_Wingman.cshtml";
    var language = ViewData["Language"].ToString();

}

@await Html.PartialAsync("MenuPartial", Model.Menu)

@if (Model.Entities != null && Model.Entities.Count > 0)
{
    <div class="main-container">

        <!--end of section-->
        <section class="flush-with-above">
            <div class="container">
                <ul class="feature-list feature-list-lg">
                    @foreach (var item in Model.Entities)
                    {
                        var link = "/" + Constants.Link.News + "/" + ViewData["Category"] + "/" + @item.Alias;
                        <li class="row justify-content-around align-items-center">
                            <div class="col-12 col-md-6 col-lg-5">
                                <a href="@link" class="d-block mb-3">
                                    @if (item.Images != null && item.Images.Count > 0)
    {
                                <img src="~/@item.Images[0].Path/@item.Images[0].FileName" class="img-fluid rounded" alt="@item.Name" />
}
else
{
                                <img src="~/images/1449046872no-img.jpg" class="img-fluid rounded" alt="@item.Name" />
}
                                </a>
                                    @*<img alt="Image" src="assets/img/graphic-product-kin.jpg" class="img-fluid rounded" />*@
                            </div>
                            <!--end of col-->
                            <div class="col-12 col-md-6 col-lg-5">
                                <h5>@item.Name</h5>
                                <p>
                                    @item.Description
                                </p>
                                <a href="@link">More about Phasers &rsaquo;</a>
                            </div>
                            <!--end of col-->
                        </li>
                    }
                </ul>

                @*<ul class="row mb-3 feature-list feature-list-sm">
                    @foreach (var item in Model.Entities)
                    {
                        var link = "/" + Constants.Link.News + "/" + ViewData["Category"] + "/" + @item.Alias;
                        <li class="col-12 col-md-6 col-lg-4">
                            <div class="card card-lg">
                                <a href="@link" class="d-block mb-3">
                                    @if (item.Images != null && item.Images.Count > 0)
                                    {
                                        <img src="~/@item.Images[0].Path/@item.Images[0].FileName" class="card-img-top" alt="@item.Name" />
                                    }
                                    else
                                    {
                                        <img src="~/images/1449046872no-img.jpg" class="card-img-top" alt="@item.Name" />
                                    }
                                </a>
                                <div class="card-body">
                                    <a href="@link">
                                        <h4 class="card-title mb-3">@item.Name</h4>
                                    </a>
                                    <p class="card-text">@item.Description</p>
                                </div>
                                <div class="card-footer card-footer-borderless">
                                    <div class="media blog-post-author">
                                        <img alt="Image" src="~/assets/wingman/img/avatar-male-1.jpg" class="avatar avatar-xs" />
                                        <div class="media-body">
                                            <small>Cameron Vasquez</small>
                                            <small>28th February 2018 &bull; 4 min read</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </li>
                    }
                </ul>*@
                <!--end of row-->
                <div class="row justify-content-center">
                    <div class="col-auto">
                        <nav aria-label="Page navigation example">
                            <ul class="pagination">
                                <li class="page-item disabled">
                                    <a class="page-link" href="#" aria-label="Previous">
                                        <span aria-hidden="true">
                                            <i class="icon-chevron-left"></i>
                                        </span>
                                        <span class="sr-only">Previous</span>
                                    </a>
                                </li>
                                <li class="page-item active">
                                    <a class="page-link" href="#">1</a>
                                </li>
                                <li class="page-item">
                                    <a class="page-link" href="#">2</a>
                                </li>
                                <li class="page-item">
                                    <a class="page-link" href="#">3</a>
                                </li>
                                <li class="page-item">
                                    <a class="page-link" href="#" aria-label="Next">
                                        <span aria-hidden="true">
                                            <i class="icon-chevron-right"></i>
                                        </span>
                                        <span class="sr-only">Next</span>
                                    </a>
                                </li>
                            </ul>
                        </nav>
                    </div>
                    <!--end of col-->
                </div>
                <!--end of row-->
            </div>
            <!--end of container-->
        </section>
        <!--end of section-->

        <section class="space-lg">
            <img alt="Image" src="~/assets/wingman/img/graphic-bg-clouds-5.png" class="bg-image" />
            <div class="container">
                <div class="row mb-4 justify-content-center text-center">
                    <div class="col-12 col-md-10 col-lg-9">
                        <h1 class="display-4">@Html.Raw(TextHelper.GetText(549, language))</h1>
                        <span class="lead">@Html.Raw(TextHelper.GetText(550, language))</span>
                    </div>
                    <!--end of col-->
                </div>
                <!--end of row-->
                <form>
                    <div class="form-row justify-content-center">
                        <div class="col-auto col-md-5 col-lg-4">
                            <div class="form-group">
                                <input class="form-control form-control-lg" type="email" name="email" placeholder="Email" />
                                <small>@Html.Raw(TextHelper.GetText(551, language))</small>
                            </div>
                        </div>
                        <div class="col-auto">
                            <button type="submit" class="btn btn-lg btn-primary">@Html.Raw(TextHelper.GetText(552, language))</button>
                        </div>
                    </div>
                    <!--end of row-->
                </form>
            </div>
            <!--end of container-->
        </section>
    </div>
}
else
{
    @await Html.PartialAsync("UpdatePartial")
}

@await Html.PartialAsync("FooterPartial", Model.Menu)
